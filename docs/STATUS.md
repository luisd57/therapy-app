# Implementation Status

- **API**: DONE. All endpoints implemented and tested; post-review security cleanups 2026-05-28; auth reverted to single-session `THERAPY_JWT` cookie 2026-06-03; timezone management 2026-08-13 (UTC `timestamptz`, practice-zone schedule blocks, instants-only request/response contract); controllers are one route action per class as of 2026-08-19 (ADR-0006), with `/api/auth` converted in PR #43, `Health` in PR #55, `Patient` in PR #56, `Therapist` in PR #57, `PublicAppointment` in PR #58, `TherapistAppointment` in PR #59 and `TherapistSchedule` in PR #61, closing out `.scratch/controller-per-action/`; the one-action rule is documented rather than test-enforced as of PR #63, while `ProtectedRouteRolesTest` still fails the build on an action under `/api/therapist` or `/api/patient` missing its `#[IsGranted]`; invitation and password reset tokens hashed at rest, and a password reset now ends sessions issued before it, 2026-08-21 (PR #50); `schedule_exceptions.reason` declared `NOT NULL` in the migration that creates it 2026-08-23 (PR #65), resolving ticket 01 of `.scratch/schema-mapping-drift/`; entities declare their ORM relations as of 2026-08-30 (ADR-0007) in [PR #74](https://github.com/luisd57/therapy-app/pull/74), so `doctrine:schema:validate` passes and `doctrine:migrations:diff` comes back empty.
- **Landing**: IN PROGRESS. Slot browser + request form, timezone-aware (viewer-zone grid, zone banner, dual-time display); modality chosen up front and carried through to submission; Vitest unit suite for date and modality helpers; Playwright E2E suite for the reservation flow, containerized.
- **Dashboard - done**: therapist login, logout, patient login, patient registration, forgot/reset password, role-based navigation, patient manager (invite/resend/revoke + patients list), Playwright E2E suite for invitation + auth flows, containerized.
- **Dashboard - next**: appointment queue, appointment list.
- **Dashboard - todo**: schedule manager, exception manager, therapist profile, patient area, Spanish translation (the therapist speaks no English, so the dashboard is currently unusable by its primary user).
- **Timezone**: 4 of 18 tickets resolved in `.scratch/timezone-management/`, most recently ticket 05 (modality is chosen before slots render, the browsed modality is the one submitted, practice-timezone fallback consolidated into `landing/src/config.ts`). Ticket 12 shipped in PR #37 but stays open on its weekend-verification criterion. Tickets 04 and 06 to 18 open; decisions in ADR-0001 to ADR-0005.
- **Test hardening**: 1 of 17 tickets resolved in `.scratch/test-suite-hardening/`, ticket 01 (the availability unit tests now build the configured Slot grid, and both the factory mapping and its `services.yaml` wiring are pinned) in PR #72. Ticket 17 opened 2026-08-29 out of that work. Counts re-measured against `main` on 2026-08-30 after PR #74: the integration-file denominator in the spec and in ticket 13 moved from 48 to 50, and every other figure held.
- **CI**: DONE. GitHub Actions - `test` job (API PHPUnit + dashboard lint/build + landing build); `e2e` job (dashboard + landing Playwright via docker-compose + `docker-compose.ci.yml`, advisory-only). `main` protected, PRs required, `test` check must pass. Both landing failures are fixed: ticket 05's in-person-only case (now exercised on every run, not one evening a week) and ticket 12's Friday-to-Sunday failure. The dashboard step is still intermittently red on a strict-mode locator clash, which matters for ticket 13.
