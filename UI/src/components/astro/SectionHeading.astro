---
interface Props {
  text: string;
  subtitle?: string;
}

const { text, subtitle } = Astro.props;
---

<div class="mb-8 text-center">
  <h2 class="text-3xl font-bold text-neutral-900">{text}</h2>
  {subtitle && <p class="mt-2 text-lg text-neutral-600">{subtitle}</p>}
</div>
